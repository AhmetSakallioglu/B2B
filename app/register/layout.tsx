import { pageMetadata } from "@/lib/site-metadata";
import { RouteMetadataLayout } from "@/lib/route-metadata-layout";

export const metadata = pageMetadata("Dealer Application Form", {
  description:
    "Apply for a Cabinetto Pro wholesale dealer account to access pricing, quotes, and ordering.",
});

export default RouteMetadataLayout;
