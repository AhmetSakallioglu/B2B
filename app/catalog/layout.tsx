import { pageMetadata } from "@/lib/site-metadata";
import { RouteMetadataLayout } from "@/lib/route-metadata-layout";

export const metadata = pageMetadata("Cabinet Catalog", {
  description:
    "Browse wholesale kitchen cabinets, finishes, and sizes for Austin-area contractors and builders.",
});

export default RouteMetadataLayout;
